Local receipts from day 1 of the proof.

- `fixture-*.json` — synthetic diffs in `examples/`
- `task2-*.json` / `task3-*.json` — receipts over private brokered-trial agent patches
  (hashes only; the diffs themselves stay in Dbrain and are not published)

Do not publish this directory until the trial diffs are replaced with public agent PRs.
